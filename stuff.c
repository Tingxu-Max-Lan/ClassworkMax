#include <stdio.h>
#include <cs50.h>
#include <math.h>

#include <string.h>

int main(void){

    //gets max


    //for loop runs i times
    bool i = false;
    if(i==false){
    for(int x = 0; x<100; x++){
        int w=0;
        while(w<x){
            printf("#");
            w++;

        }
        printf("\n");
    }
    }else{
        for(int x = 100; x>0; x--){
        int w=0;
        while(w<x){
            printf("#");
            w++;
        }
        printf("\n");
    }
    }
    bool idk = true;
    if(idk == true){
        i = !i;
    main();
    }
}